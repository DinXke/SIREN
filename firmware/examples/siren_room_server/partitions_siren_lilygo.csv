# SIREN partition table — LILYGO T-LoRa V2.1-1.6 (ESP32, 4 MB flash)
#
# Single factory app slot (NO OTA). 4 MB physical flash cannot host dual
# 1.5 MB OTA slots plus a usable spiffs region, so this board drops OTA and
# keeps persisted data on the onboard microSD (SIREN_SD_* pins) instead.
#
# WARNING: if the app slot size or SPIFFS offset must change, do a full
# erase + serial flash on the device.
#
# Name,    Type, SubType, Offset,   Size,     Flags
nvs,       data, nvs,     0x9000,   0x5000,
app0,      app,  factory, 0x10000,  0x300000,
spiffs,    data, spiffs,  0x310000, 0x0F0000,